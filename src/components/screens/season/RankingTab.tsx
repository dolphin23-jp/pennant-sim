import { Card, EmptyState, SectionTitle } from '../../ui';

export function RankingTab() {
  return (
    <Card ariaLabel="ランキング画面準備中">
      <SectionTitle>League Ranking</SectionTitle>
      <EmptyState>タイトルランキングはフェーズ2で追加します。</EmptyState>
    </Card>
  );
}
